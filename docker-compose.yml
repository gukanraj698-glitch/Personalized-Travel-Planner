# Optional only. You already have PostgreSQL installed on Windows.
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: wanderly
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
